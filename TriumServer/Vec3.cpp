#include "Vec3.h"

const SVec3 SVec3::Zero = SVec3(0, 0, 0);
const SVec3 SVec3::Up = SVec3(0, 1, 0);
const SVec3 SVec3::Right = SVec3(1, 0, 0);
const SVec3 SVec3::Forward = SVec3(0, 0, 1);
const SVec3 SVec3::Backward = SVec3(0, 0, -1);
